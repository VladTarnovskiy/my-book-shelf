import { DestroyDirective } from '.';

describe('DestroyDirective', () => {
  it('should create an instance', () => {
    const directive = new DestroyDirective();
    expect(directive).toBeTruthy();
  });
});
